# live-control-plane (Rails API) のベースURL
# 配信用のingest endpoint/stream keyはチャネル作成・選択時にこのAPI経由で取得するため、
# ここでは直接指定しない
# ※ live-control-plane はデフォルトの3000番ポートで起動する想定のため、
#   このfrontendは別ポート（例: 5500番、`npx serve -l 5500 frontend`）で配信してください
API_BASE_URL=http://localhost:3000

# 配信前の通信品質チェック用。誰でもGET/PUTできるパブリックS3バケットのベースURL
# （live-control-planeを経由せず、ブラウザから直接S3へアクセスして回線速度を計測するため）
# バケット直下に dummy_300mb.txt（ダウンロード計測用のダミーファイル）を配置しておくこと
BUCKET_BASE_URL=https://xxxxxx.s3.ap-northeast-1.amazonaws.com
